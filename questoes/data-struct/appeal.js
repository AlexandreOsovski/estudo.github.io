export const appealQuestions = [
    { topic: "Recursão (Appeal)", text: "Recursão deve ter caso base.", answer: true, explanation: "✅ VERDADEIRO! Toda função recursiva DEVE ter pelo menos um CASO BASE (ou condição de parada) que não faz chamada recursiva. O caso base resolve a instância mais simples do problema (ex: f(0)=1 em fatorial, lista vazia). Sem caso base, a recursão seria infinita (ou até stack overflow). O princípio: o problema é dividido em subproblemas menores que convergem para o caso base." },
    { topic: "Recursão (Appeal)", text: "Recursão infinita causa stack overflow.", answer: true, explanation: "✅ VERDADEIRO! Quando uma função recursiva não tem caso base ou o caso base nunca é alcançado, a recursão continua indefinidamente. CADA chamada recursiva consome espaço na pilha de chamadas (call stack) para armazenar variáveis locais, parâmetros e endereço de retorno. Eventualmente, a pilha fica cheia e lança erro: 'Stack Overflow' ou 'Maximum recursion depth exceeded'. Exemplo clássico: recursão sem condição de parada." },
    { topic: "Recursão (Appeal)", text: "Toda recursão pode ser convertida em iteração.", answer: true, explanation: "✅ VERDADEIRO! Teoricamente, qualquer função recursiva pode ser convertida para uma versão ITERATIVA usando loops e uma pilha explícita (no lugar da pilha de chamadas implícita). A equivalência entre recursão e iteração é garantida pela tese de Church-Turing (ambos são Turing-completos). Na prática, algumas recursões (como DFS em árvore) são mais naturais recursivamente; outras (como busca binária) são mais claras iterativamente. Questão de estilo." },
    { topic: "Recursão (Appeal)", text: "Recursão é sempre mais rápida que iteração.", answer: false, explanation: "❌ FALSO! Na verdade, recursão é GERALMENTE MAIS LENTA que iteração devido ao overhead: 1) função call overhead (empilhar frame), 2) alocação de variáveis locais a cada nível, 3) gerenciamento de retorno. Além disso, recursão pode causar stack overflow para entradas grandes. Iteração usa apenas um frame fixo na pilha, sendo mais eficiente e segura. Exceção: tail-call optimization (bem implementada) pode igualar, mas não é universal." },
    { topic: "Recursão (Appeal)", text: "Memoização otimiza recursão.", answer: true, explanation: "✅ VERDADEIRO! Memoização é uma técnica que ARMAZENA resultados de chamadas recursivas previamente calculadas (em cache, geralmente usando hash table ou array). Quando a mesma entrada aparece novamente, retorna o valor cacheado em vez de recalcular. Isso transforma algoritmos exponenciais (ex: Fibonacci recursivo puro: O(2ⁿ)) em lineares (O(n) com memoização). Memoização é a base da Programação Dinâmica (top-down). Trade-off: troca tempo por memória." },
    { topic: "Recursão (Appeal)", text: "Fibonacci recursivo puro é O(2^n).", answer: true, explanation: "✅ VERDADEIRO! A versão ingênua de Fibonacci (fib(n) = fib(n-1) + fib(n-2)) é exponencial O(2ⁿ). O cálculo repete os mesmos subproblemas milhões de vezes. Exemplo: fib(5) calcula fib(3) duas vezes, fib(2) três vezes, etc. A árvore de recursão tem cerca de 2ⁿ nós. Para n=40, já são ~1 bilhão de chamadas! Por isso memoização ou iteração são obrigatórios para Fibonacci." },
    { topic: "Recursão (Appeal)", text: "Fibonacci com memoização é O(n).", answer: true, explanation: "✅ VERDADEIRO! Com memoização (cache dos resultados), cada valor é calculado apenas UMA vez e armazenado. Para calcular fib(n), você precisa calcular fib(0), fib(1), fib(2), ..., fib(n) exatamente uma vez cada. Portanto, O(n). Exemplo: memo = {}; se fib(k) está no cache, retorna; senão calcula recursivamente e armazena. Também pode usar DP bottom-up: iteração com duas variáveis, também O(n)." },
    { topic: "Recursão (Appeal)", text: "Tail recursion pode ser otimizada.", answer: true, explanation: "✅ VERDADEIRO! Tail recursion (recursão em cauda) ocorre quando a chamada recursiva é a ÚLTIMA operação na função (sem operações pendentes após o retorno). Nesse caso, um compilador otimizado pode aplicar 'Tail Call Optimization' (TCO), reutilizando o frame atual da pilha em vez de criar um novo. Isso elimina o risco de stack overflow e equipara recursão à iteração em performance. Exemplo: função fatorial com acumulador, ou funções recursivas que retornam diretamente o resultado da chamada recursiva." },
    { topic: "Recursão (Appeal)", text: "Quicksort usa recursão divide-and-conquer.", answer: true, explanation: "✅ VERDADEIRO! Quicksort é o clássico exemplo de algoritmo 'dividir para conquistar' recursivo: 1) escolhe um pivô, 2) particiona array em menores e maiores, 3) chama recursivamente Quicksort nas duas metades. A recursão é natural aqui - o caso base é quando a partição tem tamanho 0 ou 1. Implementações iterativas são possíveis (usando pilha explícita), mas a versão recursiva é mais limpa e compreensível." },
    { topic: "Recursão (Appeal)", text: "Mergesort é recursivo divide-and-conquer.", answer: true, explanation: "✅ VERDADEIRO! Mergesort também exemplifica recursão: divide o array em duas metades (recursivamente), ordena cada metade, depois intercala (merge) as metades ordenadas. A recursão vai até arrays de tamanho 1 (caso base). Mergesort demonstra como recursão pode trabalhar com pós-processamento (merge após chamadas recursivas) - ao contrário do Quicksort que faz partição antes." },
    { topic: "Recursão (Appeal)", text: "DFS em árvore usa recursão naturalmente.", answer: true, explanation: "✅ VERDADEIRO! DFS (Depth-First Search) em árvores é naturalmente recursiva: para cada nó, processa o nó, depois chama recursivamente DFS nos filhos. A estrutura da árvore se alinha perfeitamente com recursão. Implementação: function dfs(node) { process(node); for each child: dfs(child); }. Caso base: quando node é null/folha. É simples, elegante e intuitivo." },
    { topic: "Recursão (Appeal)", text: "BFS é melhor implementado com recursão.", answer: false, explanation: "❌ FALSO! BFS (Breadth-First Search) é NATURALMENTE ITERATIVA com uma FILA. Tenta implementar BFS recursivamente seria estranho: recursão por natureza é profundidade (pilha), não largura. Você precisaria passar nível como parâmetro ou usar recursão mútua complexa. BFS é o exemplo clássico onde iteração com fila é muito mais clara e eficiente que recursão." },
    { topic: "Recursão (Appeal)", text: "Backtracking é forma de recursão.", answer: true, explanation: "✅ VERDADEIRO! Backtracking é ALGORITMO RECURSIVO que tenta construir uma solução incrementalmente, desfazendo escolhas (backtrack) quando atinge beco sem saída. Padrão: explore uma escolha, chame recursivamente, depois 'desfaça' (desempilhe) antes de tentar próxima escolha. Backtracking explora implicitamente árvore de busca. Clássicos: N-rainhas, Sudoku, gerador de permutações, labirintos." },
    { topic: "Recursão (Appeal)", text: "N-rainhas usa backtracking recursivo.", answer: true, explanation: "✅ VERDADEIRO! N-Rainhas (colocar N rainhas em tabuleiro N×N sem se atacarem) resolve com backtracking recursivo: coloca rainha linha por linha, valida, se válido, chama recursivamente para linha seguinte. Se não houver posição válida, retorna (backtrack). Complexidade exponencial, mas poda enorme. Problema clássico para ensinar backtracking." },
    { topic: "Recursão (Appeal)", text: "Sudoku solver usa backtracking.", answer: true, explanation: "✅ VERDADEIRO! Sudoku solver é tipicamente implementado com backtracking recursivo: tenta preencher célula vazia com números de 1-9, valida se respeita regras (linha, coluna, bloco), se válido, chama recursivamente para próxima célula; se falhar, tenta próximo número; se nenhum funciona, retorna (backtrack). Com boas heurísticas (como escolher célula com menos opções) resolve Sudoku rapidamente." },
    { topic: "Recursão (Appeal)", text: "Recursão mútua é possível.", answer: true, explanation: "✅ VERDADEIRO! Recursão mútua ocorre quando duas (ou mais) funções se chamam entre si (A chama B, B chama A). Exemplo: verificar se número é par ou ímpar recursivamente: isEven(n) = isOdd(n-1), isOdd(n) = isEven(n-1), com casos base. Útil em analisadores sintáticos (parsers) para gramáticas com produções mútuas, e em alguns algoritmos de machine learning." },
    { topic: "Recursão (Appeal)", text: "Profundidade máxima é limitada pelo stack.", answer: true, explanation: "✅ VERDADEIRO! O número máximo de chamadas recursivas aninhadas é limitado pelo TAMANHO DA PILHA (call stack) do programa/sistema operacional. Valores típicos: Python ~1000 (padrão), Java ~500-10k (depende da JVM, stack size), C++ pode configurar. Recursão profunda demais (ex: recursão para n=100000) causa stack overflow. Técnicas: 1) aumentar stack (quando possível), 2) converter para iteração, 3) tail recursion com TCO." },
    { topic: "Recursão (Appeal)", text: "Indução matemática prova recursão.", answer: true, explanation: "✅ VERDADEIRO! A correção de funções recursivas é provada por INDUÇÃO MATEMÁTICA. Passos: 1) caso base: prova que funciona para entrada menor (ex: n=0, n=1). 2) hipótese indutiva: assume que funciona para k-1, k-2... 3) passo indutivo: prova que para entrada k, usando hipótese, funciona. Essa correspondência não é coincidência: recursão é implementação computacional de indução matemática." },
    { topic: "Recursão (Appeal)", text: "Recursão linear usa uma chamada recursiva.", answer: true, explanation: "✅ VERDADEIRO! Recursão LINEAR é aquela onde CADA chamada recursiva faz NO MÁXIMO UMA chamada recursiva adicional. Exemplo: fatorial, busca binária, função de Ackermann (simplificada). A árvore de recursão é basicamente uma linha. Contrasta com recursão tree-like (ex: Fibonacci, Quicksort) onde cada chamada faz MÚLTIPLAS (geralmente 2) chamadas recursivas." },
    { topic: "Recursão (Appeal)", text: "Recursão tree-like tem múltiplas chamadas recursivas.", answer: true, explanation: "✅ VERDADEIRO! Recursão TREE-LIKE (ou ramificada) ocorre quando uma função recursiva faz MAIS DE UMA chamada recursiva a partir de uma única chamada. Exemplo: Fibonacci (2 chamadas), mergesort (2 chamadas), Quicksort (2 chamadas). Isso cria uma árvore de recursão, e muitas vezes leva a complexidade exponencial se subproblemas se sobrepõem (mas memoização resolve)." },

];